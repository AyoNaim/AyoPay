import React from 'react'
import { Modal } from './Modal'

const NavMobile = ({user} : MobileNavProps) => {
  return (
    <section className='w-full max-w-[264px]'>
      <Modal />
    </section>
  )
}

export default NavMobile